version: '3.8'

services:
  postgres_app:
    container_name: postgres_app
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRESS_PASSWORD=admin
      - POSTGRES_DB=pg_app